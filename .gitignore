# Rust extension build artifact (copied into src/ohm for src-PYTHONPATH deployments)
src/ohm/_mc_rust.so
src/ohm/_mc_rust*.so

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# OHM
*.duckdb
*.duckdb.wal

# Agent profile catalogs contain bearer tokens — never commit them
.ohm/profiles.json
.ohm/active_profile

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Virtual environments
.venv/
venv/

# Beads runtime artifacts (project-level safety)
.beads/backup/
.beads/embeddeddolt/
.beads/export-state/
.beads/export-state.json
.beads/dolt/
.beads/.env
.beads/bd.sock.startlock
.beads/dolt-server.pid
.beads/dolt-server.log
.beads/dolt-server.lock
.beads/dolt-server.port
.beads/dolt-server.activity
.beads/.beads-credential-key
.beads/daemon.*
.beads/daemon-error
.beads/*.lock
.beads/*.corrupt.backup/
.beads/sqlite*
.beads/proxieddb/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.claude/worktrees/
.opencode/
reports/

# Rust build artifacts
rust/target/
*.pdb
